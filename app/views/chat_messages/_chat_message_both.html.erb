<% if user_signed_in? %>
    <%= render 'chat_messages/chat_message_right', {target_path: target_path,image_name: image_name , image: image, content:image, created_at: created_at  } %>
  <% else %>
    <%= render 'chat_messages/chat_message_left', {target_path: target_path,image_name: image_name , image: image, content:image, created_at: created_at   } %>
  <% end %>
<% end %>
<% if shop_signed_in? %>
  <% if cuurent_shop.id == Chat.find(room_id).chat_messages.last.shop_id %>
    <%= render 'chat_messages/chat_message_right', {target_path: target_path,image_name: image_name , image: image, content:image, created_at: created_at  } %>
  <% else %>
    <%= render 'chat_messages/chat_message_left', {target_path: target_path,image_name: image_name , image: image, content:image, created_at: created_at  } %>
  <% end %>
<% end %>
